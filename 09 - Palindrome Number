class Solution {
    public boolean isPalindrome(int x) {
       int n=x;
       int temp=0;
       int rev=0;
       while(n!=0){
           temp=n%10;
           rev=rev*10+temp;
           n=n/10;
       } 
       if(x==rev && x>=0)
       return true;
       else
       return false;
    }
}
